---
title: "Quickstart"
description: "Just got your endpoint? This is a good place to start. You'll learn how to send your first RPC request, and where to go next based on what you're building."
sidebarTitle: "Quickstart"
---

import FooterLinks from '/snippets/footer-links.mdx'
import StandardRpcCard from '/snippets/cards/standard-rpc.mdx'
import SteamboatCard from '/snippets/cards/steamboat.mdx'
import DasApiCard from '/snippets/cards/das-api.mdx'
import AccountSyncCard from '/snippets/cards/account-sync.mdx'
import DragonsMouthCard from '/snippets/cards/dragons-mouth.mdx'
import WhirligigCard from '/snippets/cards/whirligig.mdx'
import FumaroleCard from '/snippets/cards/fumarole.mdx'
import HermesCard from '/snippets/cards/hermes.mdx'
import HydrantCard from '/snippets/cards/hydrant.mdx'
import OldFaithfulCard from '/snippets/cards/old-faithful.mdx'
import JetSenderCard from '/snippets/cards/jet-sender.mdx'
import PriorityFeesCard from '/snippets/cards/priority-fees-api.mdx'
import MetisSwapCard from '/snippets/cards/metis-swap-api.mdx'
import TitanSwapCard from '/snippets/cards/titan-swap-api.mdx'
import JitoBundlesCard from '/snippets/cards/jito-bundles.mdx'
import DedicatedGrpcCard from '/snippets/cards/dedicated-grpc-node.mdx'
import WhiteLabelValidatorCard from '/snippets/cards/white-label-validator.mdx'
import UseCaseTradingCard from '/snippets/cards/use-cases/trading.mdx'
import UseCaseDefiCard from '/snippets/cards/use-cases/defi.mdx'
import UseCaseWalletCard from '/snippets/cards/use-cases/wallet.mdx'
import UseCaseNftCard from '/snippets/cards/use-cases/nft-marketplace.mdx'
import UseCaseIndexerCard from '/snippets/cards/use-cases/indexer.mdx'
import UseCaseGamingCard from '/snippets/cards/use-cases/gaming.mdx'
import UseCaseAiAgentCard from '/snippets/cards/use-cases/ai-agent.mdx'

<Steps>
  <Step title="Sign up and deposit $125" titleSize="h3">
    Sign up at [customers.triton.one](https://customers.triton.one/users/sign-up), verify your email, and top up the {"$"}125 minimum (stablecoins only).
  </Step>
  <Step title="Set up an endpoint" titleSize="h3">
    Open the dashboard, click **Create endpoint**, and pick **Solana mainnet** (or devnet for testing). The portal returns two things you'll use everywhere:

    - **Endpoint URL**: `<your-endpoint>.mainnet.rpcpool.com`
    - **Secret token**: a long random string

    Keep the token server-side only. Frontend code uses an origin allowlist instead. See [Auth and security](/solana/get-started/auth-and-security). Full walkthrough: [Set up your account](/solana/get-started/account-management/platform-overview).
  </Step>
  <Step title="Send your first request" titleSize="h3">
    Call `getSlot` to confirm the endpoint is live. Pick your stack:

    <CodeGroup>

    ```bash curl
    curl https://<endpoint>.mainnet.rpcpool.com/<token> \
      -H "Content-Type: application/json" \
      -d '{"jsonrpc":"2.0","id":1,"method":"getSlot"}'
    ```

    ```javascript Solana Kit
    import { createSolanaRpc } from "@solana/kit";

    const rpc = createSolanaRpc("https://<endpoint>.mainnet.rpcpool.com/<token>");
    const slot = await rpc.getSlot().send();
    console.log(slot);
    ```

    ```javascript web3.js
    import { Connection } from '@solana/web3.js';

    const conn = new Connection(
      'https://<endpoint>.mainnet.rpcpool.com/<token>',
      'confirmed'
    );
    console.log(await conn.getSlot());
    ```

    ```python python
    import requests

    r = requests.post(
        'https://<endpoint>.mainnet.rpcpool.com/<token>',
        json={'jsonrpc': '2.0', 'id': 1, 'method': 'getSlot'},
    )
    print(r.json()['result'])
    ```

    ```rust rust
    use solana_client::rpc_client::RpcClient;

    let client = RpcClient::new(
        "https://<endpoint>.mainnet.rpcpool.com/<token>".to_string(),
    );
    println!("{}", client.get_slot()?);
    ```

    </CodeGroup>

    If you got back something like `{ "jsonrpc": "2.0", "result": 311340987, "id": 1 }`, you're connected. If you hit a 401, 429, timeout, or gRPC 403, see the [Error handling guide](/solana-guides/error-handling/how-to-troubleshoot) for the full debug flow.
  </Step>
</Steps>

## Where to next

Two ways in. Pick the tab that fits.

<Tabs>
  <Tab title="By product (I know what I want)">
    Each product is purpose-built for one job. Pick what you need.

    ### Reading state

    <CardGroup cols={2}>
      <StandardRpcCard />

      <SteamboatCard />

      <DasApiCard />

      <AccountSyncCard />
    </CardGroup>

    ### Streaming

    <CardGroup cols={2}>
      <DragonsMouthCard />

      <WhirligigCard />

      <FumaroleCard />

      <HermesCard />
    </CardGroup>

    ### History

    <CardGroup cols={2}>
      <HydrantCard />

      <OldFaithfulCard />
    </CardGroup>

    ### Sending transactions

    <CardGroup cols={2}>
      <JetSenderCard />

      <PriorityFeesCard />

      <MetisSwapCard />

      <TitanSwapCard />

      <JitoBundlesCard />
    </CardGroup>

    ### Dedicated and validator services

    <CardGroup cols={2}>
      <DedicatedGrpcCard />

      <WhiteLabelValidatorCard />
    </CardGroup>
  </Tab>
  <Tab title="By use case (I'm not sure yet)">
    Pick the kind of app you're building. Each card jumps to the matching setup guide.

    <CardGroup cols={2}>
      <UseCaseTradingCard />

      <UseCaseDefiCard />

      <UseCaseWalletCard />

      <UseCaseNftCard />

      <UseCaseIndexerCard />

      <UseCaseGamingCard />

      <UseCaseAiAgentCard />
    </CardGroup>
  </Tab>
</Tabs>

---

<FooterLinks />